# config/intrinsics.yaml

width: 640
height: 480

# Calibration expects [fx, fy, cx, cy, k1, k2, p1, p2]
calibration: [
  320.2549,     # fx  (from K[0,0])
  320.2549,     # fy  (from K[1,1])
  320.0,        # cx  (from K[0,2])
  240.0,        # cy  (from K[1,2])
  -0.05,        # k1  (from d[0])
   0.01,        # k2  (from d[1])
  -0.005,       # p1  (from d[2])
   0.001        # p2  (from d[3])
]